import { ContentTimelineView } from '@/components/admin/content-timeline-view';

export const metadata = { title: 'Content — Timeline' };

export default function ContentTimelinePage() {
  return <ContentTimelineView />;
}
