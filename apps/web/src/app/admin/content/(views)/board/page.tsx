import { ContentBoardView } from '@/components/admin/content-board-view';

export const metadata = { title: 'Content — Board' };

export default function ContentBoardPage() {
  return <ContentBoardView />;
}
