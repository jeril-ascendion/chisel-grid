{
  "name": "@chiselgrid/studio-core",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@chiselgrid/config": "workspace:*",
    "drizzle-kit": "^0.22.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
